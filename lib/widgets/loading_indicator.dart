import 'package:flutter/material.dart';

class LoadingIndicator extends StatelessWidget {
  @override
  //Widget loading for movies
  Widget build(BuildContext context) => Center(
        child: CircularProgressIndicator(),
      );
}
